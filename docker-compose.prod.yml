services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: sulama_db_prod
      POSTGRES_USER: sulama_user
      POSTGRES_PASSWORD: Ardahan91185
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped
    networks:
      - app-network
  pgadmin:
    image: dpage/pgadmin4
    container_name: pgadmin-tambis
    environment:
      PGADMIN_DEFAULT_EMAIL: info@uisoft.tech
      PGADMIN_DEFAULT_PASSWORD: ardahan91185
    ports:
      - "5050:80"
    depends_on:
      - db
    restart: always
    networks:
      - app-network

  web:
    build: .
    command: gunicorn sulama_project.wsgi:application --bind 0.0.0.0:8000 --workers 3
    volumes:
      - static_volume:/app/staticfiles
      - media_volume:/app/media
    depends_on:
      - db
    ports:
      - "8080:8000"
    environment:
      - DEBUG=False
      - SECRET_KEY=yx1!*3xq8mhsao9+4)^&!(f&3cze*#pxijips#ob1s$g&yi99$&xsandernet91185
      - DB_NAME=sulama_db_prod
      - DB_USER=sulama_user
      - DB_PASSWORD=Ardahan91185
      - DB_HOST=db
      - DB_PORT=5432
      - ALLOWED_HOSTS=199.241.137.77,localhost,127.0.0.1
    restart: unless-stopped
    networks:
      - app-network

  frontend:
    build:
      context: ./gsp-frontend
      args:
        NEXT_PUBLIC_API_URL: "http://199.241.137.77:60"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_API_URL=http://199.241.137.77:8080/
    restart: unless-stopped
    networks:
      - app-network

  nginx:
    image: nginx:alpine
    ports:
      - "60:80"
    volumes:
      - ./nginx.prod.conf:/etc/nginx/conf.d/default.conf
      - static_volume:/app/staticfiles
      - media_volume:/app/media
    depends_on:
      - web
      - frontend
    restart: unless-stopped
    networks:
      - app-network

volumes:
  postgres_data:
  static_volume:
  media_volume:
  

networks:
  app-network:
    driver: bridge 